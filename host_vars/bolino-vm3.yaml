debian_distribution: bookworm

user_home: /root

www_path: /var/www

# fail2ban configurations
fail2ban_jails:
  - name: sshd
  - name: nginx
  - name: php
fail2ban_filters: [] # No need to specify nginx filters as they're included by default

# Starship prompt configuration
starship_host_color: "bold blue"
