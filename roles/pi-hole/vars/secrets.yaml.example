# Pi-hole domain: set in host_vars per host (e.g. pihole__domain: pihole.example.com)
# Optional, in host_vars: pihole__hostname to differentiate instances.
# Pi-hole web port
pihole__web_port: 8001
# Pi-hole admin interface password
pihole__admin_password: 'your_secure_password_here'
