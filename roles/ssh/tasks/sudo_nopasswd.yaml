- name: Allow Ansible user to sudo without password
  ansible.builtin.copy:
    dest: "/etc/sudoers.d/{{ ansible_user }}-ansible"
    content: "{{ ansible_user }} ALL=(ALL) NOPASSWD:ALL\n"
    mode: u=rw,g=r,o= # 440
    validate: visudo -cf %s
  become: true
