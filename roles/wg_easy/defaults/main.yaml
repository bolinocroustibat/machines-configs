# Optional: fix VPN subnet on first container start (wg-easy uses this only at first run). Must set both if you set one.
# wg_easy__init_ipv4_cidr: "10.8.0.0/24"
# wg_easy__init_ipv6_cidr: "fd00::/64"
